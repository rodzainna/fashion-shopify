{% if footer %}
    <a href="{{ gplus_url }}" class="text-gray-300 hover:text-green-400">
        {% include 'icon-plus' %}
    </a>
    
{% else %}
    <a href="#" class="text-gray-300 hover:text-green-400">
        {% include 'icon-plus' %}
    </a>
{% endif %}